-- Gruvbox tiene el estilo 'dark' (default) o 'light', y diferentes paletas.
return {
  -- Usamos el estilo oscuro.
  style = "dark",
  -- Set de colores suave (soft), medio (medium), o duro (hard).
  palette_toggle = "soft",
  -- Activa la transparencia.
  transparent_mode = true,
}
