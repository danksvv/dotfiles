-- Rosé Pine tiene variantes 'main' (default, medio), 'moon' (oscuro), 'dawn' (claro).
return {
  -- Usamos la variante 'moon' para un aspecto suave y oscuro.
  variant = "dawn",
  -- Esta opción fuerza la transparencia del fondo.
  disable_background = true,
}
