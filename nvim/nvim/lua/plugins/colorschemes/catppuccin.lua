-- Catppuccin tiene variantes 'latte', 'frappe', 'macchiato', y 'mocha' (oscura).
return {
  -- Usamos la variante 'mocha' para un aspecto oscuro.
  flavour = "macchiato",
  -- Mantiene el fondo transparente.
  transparent_background = true,
}
